# 复制为 .env 后使用：cp .env.example .env
# .env 已被 gitignore，不入库 —— 本文件才是入库的模板。
#
# 本形态（Docker 单机）的依赖与应用跑在同一台宿主机、同一个 docker 网络（blog-net）上，
# 所以地址一律用**容器短名**，不用公网域名，也不用 localhost。
# 网络由 `make docker-deps` / `make docker-app` 自动创建。

# 镜像标签
IMAGE_TAG=latest

# Nacos 连接参数
# NACOS_NAMESPACE 取 prod：该配置集里的主机名（mysql / redis / rabbitmq / minio）
# 与本形态各容器的 service 名一致。配置的发布方式见 deploy/nacos-config/配置说明.md
NACOS_HOST=nacos
NACOS_PORT=8848
NACOS_USERNAME=nacos
NACOS_PASSWORD=nacos
NACOS_NAMESPACE=prod
NACOS_GROUP=veweiyi.cn

# 宿主机端口映射。留空则用 docker-compose.yml 里的默认值。
# 改动前先确认与前端的反代目标一致，见 deploy/nginx/conf.d/
# APP_RPC_PORT=9120
# APP_API_PORT=9420
# ADMIN_API_PORT=9421
# APP_WEB_PORT=9520
# ADMIN_WEB_PORT=9521
